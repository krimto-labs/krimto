# pnpm v11 reads build-script approvals here (no longer from package.json).
# esbuild ships a prebuilt binary via its install script; vitest/tsx need it.
allowBuilds:
  better-sqlite3: true
  esbuild: true
