# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo
coverage/

# Runtime data & derived index (facts live in a git-backed data volume; SQLite index is derived)
data/
logs/
*.db
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal

# Secrets / env
.env
.env.*
!.env.example

# OS / editor artifacts
.DS_Store
.specstory/

# Internal documents — MUST NOT be published to the public repo.
# files/ is the original planning drop; docs/ holds the strategy/spec/explainer
# dossiers. Both stay on disk for local dev tooling (subagents, CLAUDE.md
# references) but are never committed.
files/
docs/
