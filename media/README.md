# media/

Static assets the repo README links to. (The README can't reference `docs/` — that path is gitignored
and a test enforces it.)

- **`krimto-demo.gif`** — the lead demo recording: `npx @krimto-labs/krimto init` sets it up, then the
  agent saves a fact in the Claude Code panel and recalls it in Cursor — all in one window. The README's
  "See it" section embeds it. The full three-demo storyboard lives at `docs/demos/krimto-demos.html` (local).
