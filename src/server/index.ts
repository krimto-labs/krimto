// Krimto MCP server entrypoint (scaffold).
// The real implementation lands in Week 1 per the Build Spec (Gaps 01-05):
// fact schema, scope convention, retrieval pipeline, and the MCP tool surface.

export const KRIMTO_VERSION = "0.2.0";

export function main(): void {
  process.stdout.write(`Krimto ${KRIMTO_VERSION} — server not yet implemented (scaffold).\n`);
}

main();
